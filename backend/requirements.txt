# MedGraphRAG Backend — Runtime Requirements
# Install into the existing Data_Normalization/.venv (already contains torch, faiss, kuzu, transformers)

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
pyarrow>=15.0.0
numpy>=1.26.0
python-dotenv>=1.0.0

# Already installed in venv:
# torch (CPU inference only at runtime — no .cuda() calls)
# transformers
# faiss-gpu (used as CPU via faiss.read_index)
# kuzu==0.11.3
